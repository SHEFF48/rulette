import React from "react";

const Header = async () => {
  return (
    <header className="flex flex-col items-center text-[#38303F] h-full w-full">
      Header
    </header>
  );
};

export default Header;

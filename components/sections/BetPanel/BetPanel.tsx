import { BetForm } from "@/components/custom/BetForm/BetForm";
import React from "react";

const BetPanel = () => {
  return (
    <section className="w-full flex flex-col items-center justify-center gap-4">
      <BetForm />
    </section>
  );
};

export default BetPanel;
